# Tests

Cross-workspace architecture and integration tests live here. Unit tests should
remain beside the module they verify. Browser end-to-end tests must follow the
P0/P1 state transitions and release gates in
`docs/INTERACTION_TEST_PLAN.md`; viewport-only screenshots do not count as
interaction evidence.
